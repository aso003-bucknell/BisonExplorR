% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch_graded_tutorial.R
\name{launch_graded_tutorial}
\alias{launch_graded_tutorial}
\title{Open the graded (credit-bearing) version of a tutorial}
\usage{
launch_graded_tutorial(module = NULL)
}
\arguments{
\item{module}{Which module to open, e.g. \code{"module5"}. Also accepts the
folder-style name (\code{"05-groups"}) or a bare number (\code{5}). If \code{NULL}, the
available modules are listed.}
}
\value{
(Invisibly) the URL opened, or the vector of module ids when
\code{module = NULL}.
}
\description{
Each module has two ways in. \code{learnr::run_tutorial()} opens a \strong{practice}
copy inside Posit Cloud — unlimited, ungraded, and encouraged. This function
opens the \strong{deployed} copy in a browser tab, which is the one that logs
attempts for credit.
}
\details{
Call \code{launch_graded_tutorial()} with no arguments to list the modules.
}
\section{Instructor note — the URL map}{

\code{graded_urls} below is the only thing that needs editing when tutorials are
(re)deployed. Any module left as \code{NA_character_} raises an informative error
rather than opening a broken tab, so a half-deployed course fails loudly at
the module that is missing instead of silently everywhere.

The names of \code{graded_urls} are \code{tutorial_id} values (\code{"module5"}), \strong{not}
folder names (\code{"05-groups"}). Those are separate namespaces; see the seed
document. Student-facing input is normalised so all three of \code{"module5"},
\code{"05-groups"}, and \code{5} reach the same entry.
}

\examples{
\dontrun{
launch_graded_tutorial()           # list the modules
launch_graded_tutorial("module1")  # open Module 1 for credit
}
}
