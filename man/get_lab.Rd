% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_lab.R
\name{get_lab}
\alias{get_lab}
\title{Copy an in-lab R script into your project}
\usage{
get_lab(lab = NULL, overwrite = FALSE)
}
\arguments{
\item{lab}{Name of the lab script, without the \code{.R} extension
(e.g. \code{"wrangle-visualize"}). If \code{NULL}, the available labs are listed.}

\item{overwrite}{Replace an existing copy in your project? Defaults to
\code{FALSE} so you never lose work you've already done.}
}
\value{
(Invisibly) the path to the copied file, or the vector of available
lab names when \code{lab = NULL}.
}
\description{
In-lab exercises ship with BisonExplorR as plain R scripts. They live inside
the installed package, which is read-only, so this function copies one into
your current Posit Cloud project where you can open, edit, and run it with a
partner.
}
\details{
Call \code{get_lab()} with no arguments to see which labs are available.
}
\examples{
\dontrun{
get_lab()                       # list available labs
get_lab("wrangle-visualize")    # copy one into your project
}
}
