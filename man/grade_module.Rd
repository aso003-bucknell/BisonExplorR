% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grade_module.R
\name{grade_module}
\alias{grade_module}
\title{Grade a BisonExplorR tutorial module from logged responses}
\usage{
grade_module(
  responses,
  module,
  roster = NULL,
  exercises = NULL,
  completion_points = 5,
  correctness_points = 5,
  output = NULL,
  roster_cols = list(student = "student_id", section = "section")
)
}
\arguments{
\item{responses}{Path to the exported responses CSV, or a data.frame.}

\item{module}{The \code{tutorial_id} value to grade, e.g. \code{"module4"}.}

\item{roster}{Optional. Path to a CSV (or a data.frame) linking students to
sections. Needs a student-id column and a section column (see \code{roster_cols}).
When supplied, every rostered student appears in the output even if they
have no logged activity (they score 0), and output is split one sheet per
section. Students in the log but not on the roster land on an \code{UNMATCHED}
sheet.}

\item{exercises}{Optional. Either an integer count of exercises in the module,
or a character vector of the expected exercise IDs. If \code{NULL}, the function
uses the built-in \code{module_exercises} lookup, and if the module isn't there,
falls back to the distinct exercise IDs seen in the data (with a warning).}

\item{completion_points, correctness_points}{Points each half is worth.}

\item{output}{Filename stem (or path) for the CSVs. One CSV is written per
section, plus an \code{ALL} CSV and (if any) an \code{UNMATCHED} CSV. Any extension is
ignored. If \code{NULL}, a stem is generated in the working directory.}

\item{roster_cols}{Named list giving the student-id and section column names
in the roster, e.g. \code{list(student = "student_id", section = "section")}.}
}
\value{
(Invisibly) the full per-student grade data.frame. Also writes a file.
}
\description{
Reads the CSV exported from the Google Sheet that the tutorials log to,
scores each student on \strong{completion} and \strong{correctness} for one module,
and writes a spreadsheet with one worksheet per section.
}
\details{
Each tutorial exercise logs a row every time a student \strong{Runs} or
\strong{Submits} code. A Run has no grading feedback, so its \code{correct} value is
blank; a Submit records \code{TRUE}/\code{FALSE}. The two scores use that distinction:
\itemize{
\item \strong{Completion} (default 5 pts): the fraction of the module's exercises for
which the student made at least one real submission (a non-blank \code{correct}).
\item \strong{Correctness} (default 5 pts): the fraction of the module's exercises the
student answered correctly at least once (any \code{TRUE}).
}
}
